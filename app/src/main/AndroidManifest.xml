<?xml version="1.0" encoding="utf-8"?>

<!--	android:configChanges="orientation|keyboardHidden|screenSize"

		android:label="@string/app_name"
		android:exported="true"  -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="ac.kr.dankook.broken_project3" >

	
	<uses-permission android:name="android.permission.INTERNET"/>
	<application
		android:allowBackup="true"
		android:theme="@style/AppTheme"
		android:label="@string/app_name" >

		<activity
			android:name=".SplashActivity"
			android:theme="@style/SplashTheme"
			android:exported="true">

			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>

		<activity android:name=".location_activity"
			android:theme="@style/locationTheme">

		</activity>

		<activity android:name=".log_in_activity"
			android:theme="@style/loginTheme">

		</activity>

		<activity android:name=".matching_1_activity"
			android:theme="@style/matchingTheme">

		</activity>

		<activity android:name=".matching_fail_activity"
			android:theme="@style/matchingfailTheme">

		</activity>

		<activity android:name=".matching_page_activity"
			android:theme="@style/matchingpageTheme">

		</activity>

		<activity android:name=".setting2_activity"
			android:theme="@style/setting2Theme">

		</activity>

		<activity android:name=".setting_activity"
			android:theme="@style/settingTheme">

		</activity>

		<activity android:name=".sign_up_activity"
			android:theme="@style/signupTheme">

		</activity>

		<activity android:name=".sign_up_check_activity"
			android:theme="@style/signupcheckTheme">

		</activity>


	</application>
</manifest>
	
	